algoritmo "Estruturas de repetição crescente simples"

var
    contador: Inteiro
inicio
   contador <- (0)
   Enquanto (contador <= 5) faca
    EscrevaL (contador)
    contador <- (contador + 1)
   FimEnquanto
   EscrevaL ("Contei!")
fimalgoritmo
